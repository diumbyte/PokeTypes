import './Header.css'

const Header = () => {
    return (
        <header className="main-header">
            <h2 className="main-header-title">PokeTypes</h2>
        </header>
    )
}

export default Header;